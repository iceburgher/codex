# Nordic Property Lab

An offline-first Swedish property investment workspace. It compares private equity, private debt, an existing AB and a separate project AB using shared property facts and editable tax-sensitive assumptions.

## Run

```bash
npm install
npm run dev
```

Projects persist in browser storage. On the first launch only, the library receives a fully editable example project. Calculations are decision support and do not replace professional tax advice.
